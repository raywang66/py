# 色调分布对比功能 - 快速指南

## 📋 更新内容

### 新增功能
✅ 在统计窗口添加了 **"🌈 Hue Comparison"** 标签页  
✅ 类似 Lightness Distribution 的堆叠柱状图  
✅ 6个专业定义的色调区间  
✅ 支持悬停显示照片缩略图  

---

## 🎨 色调区间定义

```
  0° ──────── 10°   Very Red (太红)      深红色 ██████
 10° ──────── 20°   Red-Orange (偏红)   浅红色 ██████
 20° ──────── 30°   Normal (正常)       棕褐色 ██████
 30° ──────── 40°   Yellow (偏黄)       金黄色 ██████
 40° ──────── 60°   Very Yellow (太黄)  亮黄色 ██████
 60° ──────── 350°  Abnormal (异常)     灰色   ██████
350° ──────── 360°  Very Red (太红)      深红色 ██████
```

---

## 📊 使用流程

### 1️⃣ 分析照片
```
主界面 → 选择相册/项目 → 点击 "⚡ Batch Analyze"
```

### 2️⃣ 查看统计
```
右键相册/项目 → "View Statistics" → 切换到 "🌈 Hue Comparison" 标签
```

### 3️⃣ 交互操作
```
鼠标悬停在柱状图上 → 实时显示照片缩略图
```

---

## 💡 数据解读示例

### 正常肤色照片
```
Normal (20-30°):     ████████████████████ 60%
Red-Orange (10-20°): ████████ 25%
Yellow (30-40°):     ████ 10%
Very Red (0-10°):    ██ 3%
Very Yellow (40-60°): █ 1.5%
Abnormal (60-350°):   ▌ 0.5%
```
✅ 主要集中在 Normal 和 Red-Orange 区间，这是健康的肤色分布

### 偏黄照片（光线问题）
```
Yellow (30-40°):     ████████████████ 45%
Normal (20-30°):     ████████ 30%
Very Yellow (40-60°): ████ 15%
Red-Orange (10-20°): ██ 7%
Very Red (0-10°):    █ 2%
Abnormal (60-350°):   ▌ 1%
```
⚠️ Yellow 和 Very Yellow 占比过高，可能需要调整白平衡

### 异常照片
```
Abnormal (60-350°):  ████████████████████ 55%
Normal (20-30°):     ████████ 20%
Yellow (30-40°):     ████ 15%
Red-Orange (10-20°): ██ 7%
Very Yellow (40-60°): █ 2%
Very Red (0-10°):    ▌ 1%
```
❌ Abnormal 区间占比过高，可能是背景色污染或光照异常

---

## 🔧 技术实现

### 修改文件
- `CC_MainApp_v2.py`: 添加色调分布计算和数据库保存
- `CC_StatisticsWindow.py`: 添加 Hue Comparison 标签页和绘图方法

### 数据结构
每张照片新增6个字段：
```python
{
    'hue_very_red': float,      # 0-10°, 350-360° 的百分比
    'hue_red_orange': float,    # 10-20° 的百分比
    'hue_normal': float,        # 20-30° 的百分比
    'hue_yellow': float,        # 30-40° 的百分比
    'hue_very_yellow': float,   # 40-60° 的百分比
    'hue_abnormal': float       # 60-350° 的百分比
}
```

### 计算方法
```python
hue = point_cloud[:, 0]  # 提取色调值 (0-360°)
hue_normal = ((hue >= 20) & (hue < 30)).sum() / len(hue) * 100
```

---

## ⚙️ 配置选项

### 如果色调区间定义不适合
可以在 `CC_MainApp_v2.py` 中修改区间定义：

```python
# 当前定义
hue_very_red = (((hue >= 0) & (hue < 10)) | (hue >= 350)).sum() / len(hue) * 100
hue_red_orange = ((hue >= 10) & (hue < 20)).sum() / len(hue) * 100
hue_normal = ((hue >= 20) & (hue < 30)).sum() / len(hue) * 100

# 自定义示例（更宽的正常区间）
hue_normal = ((hue >= 15) & (hue < 35)).sum() / len(hue) * 100
```

同时需要在 `CC_StatisticsWindow.py` 中更新图例和颜色。

---

## 🐛 常见问题

### Q: 为什么有些照片没有色调分布数据？
A: 旧的分析结果没有色调分布数据，需要重新批量分析。

### Q: 色调分布图显示空白？
A: 检查是否有有效的分析数据。运行批量分析确保数据完整。

### Q: 悬停不显示照片？
A: 确保照片文件路径正确，文件未被移动或删除。

### Q: 颜色看起来不对？
A: 色调区间的颜色是示意性的，不是照片的真实颜色。

---

## 📝 对比优势

### 传统 Hue Distribution（直方图）
- ❌ 显示360个色调值，信息过载
- ❌ 难以快速理解整体分布
- ❌ 不易对比多张照片

### 新的 Hue Comparison（堆叠柱状图）
- ✅ 6个区间，清晰易懂
- ✅ 百分比显示，直观对比
- ✅ 悬停预览，使用便捷
- ✅ 专业定义，适合肤色分析

---

## 🎯 应用场景

1. **批量质量检查**: 快速识别色调异常的照片
2. **光照一致性**: 确保同一场景下照片色调一致
3. **肤色分析**: 评估肤色是否在正常范围
4. **白平衡评估**: 检测是否需要调整白平衡
5. **系列对比**: 对比不同拍摄条件下的色调分布

---

## 🚀 运行测试

```bash
# 测试色调分布功能
python test_hue_comparison.py

# 运行主程序
python CC_MainApp_v2.py
```

测试脚本会打开统计窗口，展示模拟数据的色调分布。

---

## 📧 反馈

如果有任何问题或建议，欢迎反馈：
- 色调区间定义是否合理？
- 颜色编码是否清晰？
- 是否需要更多交互功能？
